#ifndef FLASH_CODE_HPP
#define FLASH_CODE_HPP

#define DL diffNode(myDiffTree, myTree, &(myTree->data[n->lefty]), parent);

#define DR diffNode(myDiffTree, myTree, &(myTree->data[n->righty]), parent);

#endif